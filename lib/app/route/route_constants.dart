/// Named route path constants used throughout the app.
class RouteConstants {
  RouteConstants._();

  /// The home screen (clock + battery hub).
  static const String home = '/';

  /// The full app list screen (swipe up from home).
  static const String app = '/app';

  /// The up-direction thematic window (swipe up from home).
  static const String up = '/up';

  /// The down-direction thematic window (swipe down from home).
  static const String down = '/down';

  /// The right-direction thematic window (swipe right from home).
  static const String right = '/right';

  /// The left-direction thematic window (swipe left from home).
  static const String left = '/left';

  /// The in-app configuration screen.
  static const String config = '/config';
}
